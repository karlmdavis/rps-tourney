---
# Run the following command against this file to install all of the Ansible roles required by this project:
# 
#     $ ansible-galaxy install -r install_roles.yml 
# 

# https://github.com/L-P/ansible-role-acmetool
# Manages the 'acmetool' client for Let's Encrypt, which can be used to register
# and renew free SSL certificates.
#- src: L-P.acmetool
- src: 'https://github.com/karlmdavis/ansible-role-acmetool.git'
  version: prerelease
  name: L-P.acmetool
